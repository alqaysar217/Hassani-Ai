
{
  "entities": {
    "UserProfile": {
      "title": "ملف المستخدم",
      "description": "بيانات المستخدم الشخصية المخزنة في قاعدة البيانات",
      "type": "object",
      "properties": {
        "displayName": { "type": "string" },
        "photoURL": { "type": "string", "description": "رابط الصورة أو كود Base64" },
        "updatedAt": { "type": "number" }
      }
    },
    "Conversation": {
      "title": "محادثة",
      "description": "تمثل جلسة محادثة بين المستخدم وحساني",
      "type": "object",
      "properties": {
        "title": { "type": "string", "description": "عنوان المحادثة" },
        "userId": { "type": "string", "description": "معرف المستخدم صاحب المحادثة" },
        "updatedAt": { "type": "number" },
        "pinned": { "type": "boolean", "description": "هل المحادثة مثبتة في الأعلى" }
      },
      "required": ["title", "userId", "updatedAt"]
    },
    "Message": {
      "title": "رسالة",
      "description": "تمثل رسالة مفردة داخل المحادثة",
      "type": "object",
      "properties": {
        "role": { "type": "string", "enum": ["user", "assistant"] },
        "content": { "type": "string" },
        "type": { "type": "string", "enum": ["text", "image", "code", "diagram", "music", "planning"] },
        "timestamp": { "type": "number" },
        "metadata": {
          "type": "object",
          "properties": {
            "code": { "type": "string" },
            "explanation": { "type": "string" },
            "mediaUrl": { "type": "string" },
            "diagramSyntax": { "type": "string" },
            "diagramExplanation": { "type": "string" }
          }
        }
      },
      "required": ["role", "content", "type", "timestamp"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "ملفات تعريف المستخدمين"
    },
    "/conversations/{conversationId}": {
      "schema": "Conversation",
      "description": "مجموعة المحادثات الرئيسية"
    },
    "/conversations/{conversationId}/messages/{messageId}": {
      "schema": "Message",
      "description": "الرسائل التابعة لمحادثة معينة"
    }
  }
}
